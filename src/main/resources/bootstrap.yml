spring:
  profiles:
    active: ${SPRING_CONFIG_PROFILE:local}
  cloud:
    config:
      enabled: ${SPRING_CONFIG_ENABLED:false}
      allowOverride: false
      failFast: true
      overrideNone: false
      overrideSystemProperties: false
      uri: ${SPRING_CONFIG_PATH:http://configurations.101digital.io:8888}
      label: ${SPRING_CONFIG_LABEL:master}
encrypt:
    failOnError: true
